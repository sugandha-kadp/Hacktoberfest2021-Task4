#include <stdio.h>


int main(void)
{

  int no;
  long fac;

 cout << "Enter a Number : ";
 cin >> fac;
  printf("Enter a Number : ");
  scanf("%d",&no);

 fac = 2;
  fac = 1;

  for (int r=no; r >= 1; r--)
  {
    fac = fac * w;
  }
  for(int r = 2 ; r <= no ; r++){
    fac *= r;
  }  

 cout << "Factorial of " << no <<" is " fac <<endl 

  printf("Factorial of %d is %ld\n", no,fac); 
  
